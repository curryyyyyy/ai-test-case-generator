"""执行闭环：把用例转成可运行脚本，并把运行结果回收为结构化执行记录。

工具侧不假设存在被测环境（没有 base_url、账号、浏览器），因此这里的定位是：

    1. 生成**可执行骨架**：一条用例一个 test 函数，函数名带 TC 编号，
       步骤与预期以 docstring 形式内联，接口/定位细节留 TODO 由使用者补齐；
    2. 回收**执行结果**：解析 pytest 输出（或手工录入的 TC × 结果表），
       归一化成 ExecutionRecord，供 Bug 分析与回归阶段消费。

刻意不生成"看起来能跑但调不通"的接口调用——编造的 URL 与参数比明确的
TODO 危害更大，那会让执行结果里的失败全部变成噪音。
"""
